version: '2'

services:
  api:
    build: .
    image: thekevjames/league-utils-api
    environment:
      - CHAMPIONGG_TOKEN=${CHAMPIONGG_TOKEN}
      - RIOT_TOKEN=${LEAGUE_TOKEN}
      - SENTRY_DSN=${SENTRY_DSN_LEAGUE}
    ports:
      - "28006:8080"
